{
	"name": "corona-obsidian-theme",
	"version": "1.0.0",
	"scripts": {
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"build": "node build.mjs --copy",
		"dev": "fswatch -o css/ | xargs -n1 -I{} node build.mjs --copy"
	}
}
